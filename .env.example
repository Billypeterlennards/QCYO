# Copy this file to .env and adjust values for your environment.
# app.py reads these via os.environ - never commit the real .env file.

# Set to 1 to enable Flask's interactive debugger for LOCAL DEVELOPMENT ONLY.
# Never enable this in production: it allows arbitrary code execution.
FLASK_DEBUG=0

# Port the API listens on
PORT=5000
